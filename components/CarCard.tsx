"use client";

import { CarCardProps } from "@/types";
import React, { FC, useState } from "react";

const CarCard: FC<CarCardProps> = ({ car }) => {
  const [] = useState();
  return (
    <div>
      <h1>this is car card</h1>
    </div>
  );
};

export default CarCard;
